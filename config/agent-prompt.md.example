<!-- 프로젝트 공용 에이전트 프롬프트 (예시).
     이 파일을 `agent-prompt.md` 로 복사하면 모든 역할의 시스템 프롬프트에
     "Project instructions" 로 추가된다. 배포(프로젝트)마다 다르게 가져가는 자리다.
     절대 규칙(날조 금지·승인 없는 쓰기 금지 등)은 여기서도 무를 수 없다. -->

- 우리 팀 마감 제안은 금요일을 피한다 (주말 넘김 방지).
- Bug 티켓에는 항상 재현 환경(브라우저/기기)을 묻는다.
- 진척 보고에는 반드시 전주 대비 변화를 포함한다.
