# Lake Task Manager - core app deps (mock / local)
# Direct runtime dependencies use exact, non-yanked stable pins so an existing venv is
# upgraded deterministically instead of remaining on any older version that satisfies a floor.
fastapi==0.141.1
uvicorn[standard]==0.52.3
requests==2.34.2
PyYAML==6.0.3
# 댓글 이미지 첨부(멀티파트 form) — FastAPI UploadFile/File 이 요구. 없으면 앱이 시작조차 안 됨.
python-multipart==0.0.32

# FastAPI request models and Agent contracts import Pydantic directly.
pydantic==2.13.4

# Windows named launcher의 VERSIONINFO를 안전하게 갱신해 작업 관리자 표시명을 LTM으로 고정.
pywin32==312; sys_platform == "win32"

# local(fake) 서버 = 외부 오픈소스 mock (이 프로젝트 world 주입). https://pypi.org/project/jira820
# 0.5.0+: 멀티프로젝트(이슈별 project) + 통합검색(JQL text~ / Confluence CQL) 필요.
# 0.12.0+: Confluence **본문** 읽기(content/{id} + ?expand=body.storage). 이게 없으면
#   read_document 가 excerpt 만 받아 조용히 빈 본문을 돌려준다 — 에러도 안 난다.
# 0.12.0은 현재 최신 stable이자 필요 기능의 최초 버전이다. exact pin이
# 기존 venv의 0.11.x도 런처 `pip install -r` 에서 반드시 교체하게 한다.
jira820==0.12.0

# ── AI 에이전트 (LTM Agent) ────────────────────────────────────────────────
#
# ★ 예전에는 `requirements-agent.txt` 로 **따로** 뒀다. 대시보드만 쓰는 사람에게 200MB+
#   의존을 강요하지 않으려던 것인데, 실제로 벌어진 일은 이랬다:
#     · 런처(배포 repo 의 run-core.ps1)는 `requirements-sso.txt` → `requirements.txt` 만 깐다
#     · 그래서 앱을 켜면 AI 탭이 "AI 에이전트가 켜져 있지 않습니다 — pip install …" 화면
#     · 유저의 배포 repo 는 **대개 최신이 아니라**, 런처를 고쳐도 그 유저에게는 안 간다
#   설치 안내를 읽고 손으로 pip 를 치는 유저는 없다. 기능을 껐다 켜는 스위치가 아니라
#   **그냥 안 되는 기능**이 된다.
#
#   그래서 합친다. 여기 있으면 **런처를 안 고쳐도, 배포 repo 가 낡았어도** 다음 실행에
#   깔린다 — 이미 나가 있는 모든 런처가 이 파일을 읽기 때문이다.
#   (키가 없으면 AI 탭만 비활성이고 나머지 화면은 그대로 쓴다 — 그 게이팅은 그대로다.)
#
# 과제용 0.3.x/1.x SDK 조합은 폐기했다. `langchain`은 Langfuse callback이,
# `langchain-community`는 현재 FAISS vectorstore adapter가 직접 사용하므로 지연 import까지
# 포함해 명시한다. community sunset 후속으로는 독립 FAISS adapter 이주를 추적한다.
langchain==1.3.15
langchain-core==1.5.6
langchain-openai==1.5.1
langchain-community==0.4.2
langgraph==1.2.11
# ToolNode를 production에서 직접 import한다. LangGraph의 범위 전이 의존에 기대지 않고
# 함께 검증한 prebuilt 구현을 재현 가능하게 고정한다.
langgraph-prebuilt==1.1.0

# Instructor 1.15.4의 openai>=2,<3 제약 안에서 가장 최신 stable 2.x.
openai==2.54.0

# Pydantic structured-output validation/retry adapter. OpenAI 2 공개 API와 함께
# 검증한 최신 stable 릴리즈를 재현 가능하게 고정한다.
instructor==1.15.4
# Instructor(jiter>=0.6.1,<0.15) × OpenAI(jiter>=0.10,<1) 교집의 최신 stable.
jiter==0.14.0

# RAG — 로컬 임베디드 벡터 스토어(서버 불필요). 인덱스는 파일로 떨어진다.
faiss-cpu==1.15.0

# 토큰 수 계산(비용 표시 + 과다 입력 차단)
tiktoken==0.14.0

# LLM 호출 트레이싱. 자체 호스팅/클라우드 모두 가능하며, 없어도 앱은 돈다(설정 안 하면 비활성).
langfuse==4.14.4

# 웹 검색(무키·무등록). 명시적 CA bundle을 써 제한된 Windows 프로세스에서도
# 사용자 인증서 저장소를 열지 않는다. 일반 기술 지식 보강용이며 폐쇄망이면 자동 무력화.
httpx==0.28.1
certifi==2026.7.22
# Windows embedded Python has no system IANA database. JQL relative-date normalization uses the
# Jira user's timeZone, so zoneinfo needs the official portable database as a runtime dependency.
tzdata==2026.3

# Agent structured-output 검증 코드가 직접 import한다. MCP의 전이 의존성에 기대지 않고
# 검증 구현과 버전을 명시적으로 고정한다.
jsonschema==4.26.0

# Rust/html5ever(Ammonia) 기반 editor HTML allowlist sanitizer. MIT, Windows abi3 wheel.
nh3==0.3.6

# MCP 서버(선택요소 B) — LTM 도구를 외부 AI 클라이언트(Claude Desktop 등)에 개방
mcp==2.0.0

# Prod (corporate SSO) path additionally needs Playwright:
#   pip install -r requirements-sso.txt   (then `playwright install chromium`)
