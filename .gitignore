# 사내 자격증명 / SSO 세션 — 절대 커밋 금지
jira_state.json
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# SQLite 캐시 (생성물)
*.sqlite3

# 빌드 중간산출물 (PyInstaller) — exe 자체는 커밋(추적)
/build/
/dist/
src/build/
src/dist/

# OS / editor
.DS_Store
Thumbs.db
