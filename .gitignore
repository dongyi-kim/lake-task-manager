# SSO 세션 — 실 크리덴셜, 절대 커밋 금지
jira_state.json
.appwin-profile/          # 앱 창 Chromium 프로필(쿠키·세션) — 절대 커밋 금지
sso/                      # 서비스별 SSO 세션(쿠키) — 절대 커밋 금지

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# SQLite 캐시 (생성물)
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm

# 빌드 산출물 (PyInstaller) — exe 는 배포 repo(lake-task-manager-deploy)에서 관리
/build/
/dist/
*.exe

# OS / editor
.DS_Store
Thumbs.db
