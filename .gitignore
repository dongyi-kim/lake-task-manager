# SSO 세션 — 실 크리덴셜, 절대 커밋 금지
jira_state.json
# .env(dev/prod/example)는 추적한다(사용자 요청). 실값이 담기는 exe-옆 런타임 .env 만 제외.
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# SQLite 캐시 (생성물)
*.sqlite3

# 빌드 산출물 (PyInstaller) — exe 는 배포 repo(lake-task-manager-deploy)에서 관리
/build/
/dist/
*.exe

# OS / editor
.DS_Store
Thumbs.db
